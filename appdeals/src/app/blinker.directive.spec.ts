import { BlinkerDirective } from './blinker.directive';

describe('BlinkerDirective', () => {
  it('should create an instance', () => {
    const directive = new BlinkerDirective();
    expect(directive).toBeTruthy();
  });
});
