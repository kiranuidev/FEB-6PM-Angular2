 const iamDON="ES6";
function printData() {
    var x = 10;
    let tryme="1000";
    if (x == 10) {
        //we are in the if block
        let y =100;
        var z=1000;
        console.log(tryme);r
    }
    //once we are out of if block
    console.log(y);
}
printData();