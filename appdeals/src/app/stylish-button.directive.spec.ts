import { StylishButtonDirective } from './stylish-button.directive';

describe('StylishButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new StylishButtonDirective();
    expect(directive).toBeTruthy();
  });
});
